{
  "name": "ols-learning",
  "version": "1.1.0",
  "private": true,
  "description": "OLS — Omani Learning System. Interactive learning platform for grades KG–12 (static single-page app + zero-dependency Node server).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node -e \"console.log('No build step required — static site')\""
  },
  "engines": {
    "node": ">=14"
  },
  "dependencies": {}
}
